<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:title="@string/app_name"
    android:id="@+id/tool_bar_completo"
    android:layout_width="match_parent" android:layout_height="wrap_content"  android:background="@color/ColorPrimaryDark" android:elevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="30dp"
            android:text="@string/app_name"
            android:id="@+id/toolbar_title"
            android:textColor="@color/ColorSecondaryLight"
            android:textSize="@dimen/abc_text_size_large_material" />

        <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/barra_btnClose"
            android:src="@drawable/ic_close"
            android:background="@android:color/transparent"
            android:paddingRight="@dimen/size3"
            android:layout_alignParentRight="true"/>
    </RelativeLayout>

   <!-- <ImageView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_favorite"
        android:id="@+id/imgFavorite"
        android:paddingLeft="450dp" />-->

</android.support.v7.widget.Toolbar>